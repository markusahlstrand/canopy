{
  "name": "@canopy/docworker",
  "version": "0.0.0",
  "description": "Document parsing adapter for Canopy — ranged PDF text, PDF outline, and spreadsheet tables behind a runtime-agnostic interface (pure-Node in-process impl; a Cloudflare Container is just another backend).",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "unpdf": "^1.6.2",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^3.2.4"
  }
}
