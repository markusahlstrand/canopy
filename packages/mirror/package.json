{
  "name": "@canopy/mirror",
  "version": "0.0.0",
  "description": "Shared, dependency-free read logic for Canopy's offline metadata mirror \u2014 the MirrorFile row shape, virtual-folder path helpers, the folder-view derivation, and delta application. Runs identically over D1 rows on the server and the IndexedDB mirror in the browser.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --dir src"
  },
  "devDependencies": {
    "typescript": "~7.0.2",
    "vitest": "^5.0.1"
  }
}
